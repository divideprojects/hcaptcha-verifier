{
  "private": true,
  "name": "hcaptcha-verifier-workers",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --config .prettierrc 'src/**/*.{ts,js,json,md,css}' --write",
    "dev": "npx wrangler dev -l --ip 127.0.0.1",
    "publish": "npx wrangler publish --minify src/index.ts"
  },
  "author": "Divanshu Chauhan <23524935+Divkix@users.noreply.github.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.19.1"
  },
  "dependencies": {
    "hono": "^2.0.4",
    "youtube-sr": "^4.2.0"
  }
}
